# Enable auto-conversion for all text files (normalizes to LF in the repo)
* text=auto

# Ensure specific critical files ALWAYS use LF endings natively on checkout
*.sh text eol=lf
*.env text eol=lf
*.conf text eol=lf
Dockerfile* text eol=lf
Makefile text eol=lf
